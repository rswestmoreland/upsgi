# R1 asset bundle

Baseline receiver path fixture with valid uncompressed JSON posts to /push.

Contents:
- shared receiver app in `../common/receiver_app.psgi`
- shared corpus and client helpers in `../common/`
- `config.yaml.in` for the scenario
- `r1_matrix.json` for the scenario matrix
- `run_r1_smoke.sh` lightweight smoke runner
