# R6 asset bundle

Hostile-ingest fixture with malformed payloads plus raw HTTP abuse cases.

Contents:
- shared receiver app in `../common/receiver_app.psgi`
- shared corpus and client helpers in `../common/`
- `config.yaml.in` for the scenario
- `r6_matrix.json` for the scenario matrix
- `run_r6_smoke.sh` lightweight smoke runner
