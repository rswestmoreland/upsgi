# W4 runtime validation seed assets

These files are carried forward from the accepted W4 validation handoff so the broader performance harness can reuse their patterns.

Included assets:
- async_getline_validation_app.psgi
- check_async_getline_hash.py
- run_runtime_validation.sh
- slow_reader_client.py

These seed assets are the basis for:
- validation-runner pattern reuse
- deterministic content-check pattern reuse
- slow-reader client pattern reuse
