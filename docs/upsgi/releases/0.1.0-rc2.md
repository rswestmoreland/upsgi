# upsgi 0.1.0-rc2 release notes draft

## Summary
This draft release marks RC2 for the PSGI-first upsgi fork.

## Highlights
- RC1 regression and fault coverage remains green
- the two narrow soak gates for RC2 passed:
  - `tests/fork/soak/request_burst_smoke.sh`
  - `tests/fork/soak/reload_cycle_smoke.sh`
- soak scripts were hardened during RC2 with pidfile tracking and longer reload polling
- migration, packaging, deferred-scope, and release docs remain in place

## RC2 gate result
- result: PASS
- required narrow soak gates passed against the built binary
- broader soak coverage remains advisory until later release cycles

## Known deferred areas
See `docs/upsgi/DEFERRED_SCOPE.md`.

## Waived non-blocking note
- The binary still reports the upstream-style version string `2.1-dev`. This is acceptable for RC2 because the gate requires a version string, not final public branding. Resolved in the 0.1.0 public release.
