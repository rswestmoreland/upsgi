# upsgi 0.1.0-rc1 decision record

- stage: RC1
- result: PASS
- profile: default PSGI-first build

## Required gate results
### Build
- default PSGI-first build present
- default embedded logging bundle confirmed:
  - `psgi`
  - `logfile`
  - `logsocket`
  - `rsyslog`
- binary reports a version string
- default build does not depend on non-Perl runtime plugins in the embedded set

### Behavior
- HTTP socket serving works
- PSGI app loading works
- `--static-map` works
- `--log-x-forwarded-for` works
- `http-modifier*` compatibility flags remain inert
- `perl-no-die-catch` remains inert
- `--log-exceptions` enables explicit PSGI exception visibility

### Tests
Required RC1 regression tests passed:
- `tests/fork/regression/startup.t`
- `tests/fork/regression/compatibility.t`
- `tests/fork/regression/exceptions.t`
- `tests/fork/regression/static.t`

Additional regression/fault checks passed during RC1:
- `tests/fork/regression/logging.t`
- `tests/fork/regression/upload.t`
- `tests/fork/regression/reload.t`
- `tests/fork/fault/bad_app_path.t`
- `tests/fork/fault/unknown_option.t`

### Docs and packaging
- README reflects PSGI-first identity
- quickstart, migration, packaging, deferred-scope, logging, compatibility, and release docs exist
- release-facing examples exist under `examples/upsgi/`

## Advisory soak record
- request burst scenario passed against the built binary in a practical reduced-count local run
- reload cycle scenario passed against the built binary in a practical reduced-count local run
- shell-script follow-up applied during RC1:
  - corrected repo-root resolution in both soak scripts
  - added optional execution knobs:
    - `UPSGI_SOAK_BURST_COUNT`
    - `UPSGI_SOAK_RELOAD_CYCLES`

## Waived non-blocking note
- The binary currently reports the upstream-style version string `2.1-dev`. This is acceptable for RC1. Resolved in the 0.1.0 public release.
