# 0.1.2 hotfix build note

This hotfix corrects two source-tree build blockers in the public 0.1.2 upload:

- adds the missing declaration for `upsgi_validate_runtime_tunables()`
- fixes a broken quoted format string in `upsgi_opt_log_drain_burst()`
