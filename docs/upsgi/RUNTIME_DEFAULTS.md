# Runtime defaults

- thunder lock is enabled by default
- body scheduler is enabled by default
- logging queues are enabled by default
- static-map remains part of the supported baseline

Use these disable knobs for default-on features:

- `--disable-thunder-lock`
- `--disable-body-scheduler`
- `--disable-log-queue`

Default-off features use the `enable-*` pattern.
